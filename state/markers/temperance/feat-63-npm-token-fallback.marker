# Temperance marker — bassclef-cli#63 fix

- date: 2026-09-12
- branch: feat/63-npm-token-fallback
- scope-decision:
  - right thing: workflow can publish without trusted publisher; operator approves environment gate from GitHub, not Touch ID on primary Mac
  - right way: add NODE_AUTH_TOKEN env sourced from NPM_TOKEN secret; drop --provenance temporarily; pin the shape with Tier 0 tests; cite #62 in the comment so future refactor knows why provenance is off
  - drift trigger: scope creeps to auto-approve the environment gate (violates manual-prod-approval.md), or removes the environment gate, or attempts to fix the trusted-publisher 404 in this PR (that's #62)
- in-scope: publish.yml env + provenance flag + step-name rename + comment block + 3 new Tier 0 tests
- out-of-scope: root-cause #62 diagnosis; 0.1.3 bump; Cooper #1 deprecation notice; any change to the environment gate
- luminaries: linus-torvalds (don't break adopters — provenance loss is documented and reversible), michael-nygard (fail-visible — comment cites #62 so next reader knows why), saltzer-schroeder (complete-mediation — Tier 0 tests pin the shape so a future refactor can't silently regress)

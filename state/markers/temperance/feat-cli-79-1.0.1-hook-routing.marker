# Temperance marker — cli 1.0.1 hook routing
- date: 2026-09-13
- branch: main
- scope-decision: right thing — cli 1.0.1 pins v0.40.0 + routes hook binaries per settings.json prefix ($HOME vs $CLAUDE_PROJECT_DIR); right way — full OOAD chain (10 steps) + Tier 0 RED harness before code touch; drift trigger — any risk not folded into an engineering action by Step 9 → pause + escalate to operator
- operator-confirmed at: session 2026-09-13d turn "yes that's fine and add an /rfc once plan is developed"

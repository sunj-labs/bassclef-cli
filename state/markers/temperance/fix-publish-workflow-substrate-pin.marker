# Temperance marker — fix/publish-workflow-substrate-pin

- date: 2026-09-21
- branch: fix/publish-workflow-substrate-pin
- scope-decision: Update `.github/workflows/publish.yml` — bump the pinned bassclef sibling tag from v0.45.0 to v1.1.0. Bump `@thebassclef/lite` from 1.4.0 to 1.4.1 (patch — fixing what 1.4.0 CHANGELOG claimed to ship). Stage PR for operator merge + tag + publish.
- right-thing: Yes. Published 1.3.0 and 1.4.0 both ship v0.45.0 substrate despite CHANGELOGs claiming v1.0.0 and v1.1.0 respectively. Peer's Ship 1 cures + F1b cure never reached adopters. Verified: tarball has `release/SKILL.md`, `journal-export/SKILL.md`, 10 runtime `/Users/` hits, no `_lib/wirings.sh`. Retraction posted at #1861 comment 5758242378.
- right-way: Update 6 occurrences of v0.45.0 in publish.yml (L111 comment, L125 ref, L140-141 assertion, L262 comment, L268 ref). Bump version. Update CHANGELOG. Tests green. Stage PR.
- blast-radius: Adopter-observable. After merge + tag + publish, adopters running `npm install -g @thebassclef/lite@1.4.1` (or latest) receive actual v1.1.0 substrate for the first time. Reversible via unpublish inside 72h.
- drift-trigger: If the publish workflow's assertion at L140 still says the tag is unexpected after edits, pause and diagnose the mismatch before push.
- upstream-dependency: bassclef v1.1.0 tag at public bassclef `b7613c5f`. Already verified fetched.

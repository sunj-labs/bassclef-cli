# Reviewer pass — 2026-08-06 WU-2 init

mode: sequential (autonomous orchestrator-gated overnight)
reviewed_at: 2026-08-06T07:36:00Z
source_files_changed: 8
files:
  - src/commands/init.ts (was stub; now real command + writeManifest + shouldRefuseRoot)
  - src/commands/init-argv.ts (new — renamed from src/lib/argv.ts per pattern-review concern #1)
  - src/commands/init-templates/settings-json.ts (new — MINIMAL template, no peer-bassclef assumption)
  - src/commands/init-templates/substrate-config-md.ts (new — YAML front-matter with template version)
  - src/commands/init-templates/manifest-json.ts (new — records what init wrote for sync workunit)
  - src/lib/resolve-target-dir.ts (new — path resolver + safety checks)
  - src/lib/write-safely.ts (new — atomic write function + mkdirSafely; single audited mutation point)
  - src/commands/sync.ts (updated — stub message; WU-3 references removed)
  - src/cli.ts (updated — routes init flags, removed WHAT-comment)

## Checks

- **Spec acceptance criteria:** PASS for WU-2-scoped items in bet
  `docs/iteration-bets/2026-08-06b-launch-npm-thebassclef-core.md`
  L122. Verified: `bassclef init` writes settings.json +
  substrate.config.md into project directory (kilo.json dropped per
  decomposition N1 — Kilo schema unknown this session, deferred to
  a later WU when the schema lands); idempotent on re-run (48 tests,
  including partial-state cases); Tier 0 tests on fixture repos
  under HOME.

- **Tests present + green:** PASS. 48 tests across 6 files.
  Confirmed via `npm test` before commit: `Tests 48 passed (48)`.

- **Test-after pattern check:** NONE FOUND. Every test file opens
  with a Beck-style test list; every listed item is covered by an
  assertion. `resolve-target-dir.test.ts` explicitly names two
  deferred items (uid-mismatch fixture) in the test-list comment
  rather than marking them `[x]` — closes the pattern-review
  concern #6.

- **ADR compliance:** PASS. ADR-002 landed alongside the code. ADR
  invariants list expanded per architect-review concern #2:
  now names default behaviors, files written, escape-hatch matrix,
  exit codes. Any change to a listed item is a MAJOR bump.

- **Designer sign-off:** N/A. No UX surface beyond CLI text
  strings. All output ran through a plain-language check — banned
  words scan in `init.test.ts` L177 confirms no bassclef jargon in
  prose output (paths excluded — `substrate.config.md` is the
  file's name per the bet).

- **/pattern-review invoked:** INVOKED via CodeReviewer subagent.
  Report at `docs/session-logs/2026-08-06-wu-2-init.md` (below).
  Applied fixes:
  - Wired `refused` outcome into symlink case (was dead enum value)
  - Removed WHAT-comment at `src/cli.ts:66`
  - Marked resolveTargetDir uid-mismatch test as deferred
  - Renamed argv module to init-argv (concern #1)

- **/architect-review invoked:** INVOKED via CodeReviewer subagent.
  3 DEFECTs + 4 CONCERNs. Applied fixes:
  - DEFECT #7 (ship-blocker) — dropped peer-bassclef assumption
    from settings.json template. Template is now MINIMAL.
  - DEFECT #3 — added init.manifest.json with template versions +
    outcome per file for sync workunit forward compatibility
  - DEFECT #4 — updated CLAUDE.md with current state, shipped
    commands, and ADR references
  - CONCERN #5 (complete mediation) — added `mkdirSafely` to close
    the mkdirSync bypass; every filesystem mutation flows through
    `write-safely.ts`
  - CONCERN #5 (test coverage) — extracted `shouldRefuseRoot` as
    pure function + added `should-refuse-root.test.ts` (4 tests
    covering uid=0/uid>0/undefined × allowRoot true/false)
  - CONCERN #2 (ADR invariants) — expanded the semver-lock list
    to name files, escape-hatch matrix, exit codes
  - CONCERN #1 (argv shape) — renamed to `src/commands/init-argv.ts`
    (command-local)

## Findings

- NOTE: Post-write verification (mentioned in decomposition L119
  as a Cooper risk mitigation) is NOT shipped. The ADR does not
  promise it; deferred to a later WU when we see evidence of
  silent-partial-write failures in the wild.
- NOTE: No `bassclef uninit` command. The manifest enables one
  cleanly; adding it in a later WU is safe (reads a well-formed
  document, reverses the writes).
- NOTE: Peer-bassclef checkout is no longer referenced by the
  default template. The sync workunit will populate substrate
  references when it lands.
- NOTE: `substrate.config.md` file name is a bassclef term of art
  from the bet — kept for compatibility. Banned-word scan in
  tests strips file paths before scanning prose.

acknowledged: true

# Reviewer pass — 2026-08-06 WU-4 publish

mode: sequential (autonomous orchestrator-gated)
reviewed_at: 2026-08-06T20:44:00Z
source_files_changed: 4
files:
  - .github/workflows/publish.yml (new — one-job workflow at semver-locked path)
  - scripts/validate-tag.mjs (new — string-equal tag check + semver format + ancestor guard)
  - scripts/andon-scan.mjs (new — operator-private term scanner)
  - scripts/tier-filter.mjs (new — YAML frontmatter tier: upstream refuser)

## Checks

- **Spec acceptance criteria:** PASS for WU-4-scoped items in bet
  `docs/iteration-bets/2026-08-06b-launch-npm-thebassclef-core.md`
  L124. Verified: publish pipeline strips `tier: upstream` files
  (L156 — tier-filter), publish pipeline runs andon scan for
  operator-private terms (L157 — andon-scan), publish uses trusted
  publisher + provenance attestations (L158 — publish.yml).
  Scope-reduced: substrate bundling + lite-manifest.json read
  deferred to a later workunit per decomp L82-92 and ADR-004
  §Cross-repo. Cross-checked against bet acceptance L155-158 —
  none of the acceptance items require the manifest read; deferral
  is honest.

- **Tests present + green:** PASS. 110 tests across 14 files.
  Confirmed via `npm test` before commit: `Tests 110 passed (110)`.

- **Test-after pattern check:** NONE FOUND. Every test file opens
  with a Beck-style test list; every listed item traces to an
  assertion. Coverage gaps flagged by pattern-review (item 7 —
  missing andon-scan allowlist test, item 5 — missing
  workflow-path test) both filled inline.

- **ADR compliance:** PASS. ADR-004 landed with the full
  invariants list expanded per architect-review item 2 + item 8:
  workflow path, triggers, permissions, ordered checks, dist-tag
  rule, exit codes, tag-format regex, refusal message shape,
  tier-filter YAML normalization. Any listed item is a MAJOR bump.

- **Designer sign-off:** N/A. No UX surface beyond CLI text +
  workflow step summaries + operator playbook prose.

- **/pattern-review invoked:** INVOKED via CodeReviewer subagent.
  1 DEFECT reclassified to PASS during review (semver regex correctly
  rejects leading zeros). 2 CONCERNs applied inline:
  - Item 6 (tier-filter YAML tolerance): extended parser to handle
    CRLF, UTF-8 BOM, leading blank line, quoted values (single +
    double). Added 5 new tests covering each case.
  - Item 7 (missing andon-scan allowlist test): added 2 tests
    exercising the FILE_ALLOWLIST branch (package.json author
    email + LICENSE inline email).

- **/architect-review invoked:** INVOKED via CodeReviewer subagent.
  1 DEFECT + 4 CONCERNs. Applied fixes:
  - DEFECT item 5 (missing workflow-path Tier 0 test): added
    `tests/workflow-path.test.ts` with three assertions — file
    exists at semver-locked path, workflow references its scripts
    by relative path, permission set is minimal (id-token + read
    only; no write elevations).
  - CONCERN item 2 (tag-format regex not in invariants): added to
    ADR-004 §Invariants.
  - CONCERN item 4 (audit-log check not called out): added a full
    "Audit habit" section to publish-setup.md naming both the
    GitHub Actions run history AND the npm activity log as
    post-publish checks.
  - CONCERN item 8 (refusal message + tier-filter normalization
    not in invariants): added both to ADR-004 §Invariants.
  - CONCERN item 9 (playbook friction): added a note that step 3
    trusted-publisher config only appears after step 2 reserves
    the name; fixed the "72 hours vs 24 hours" contradiction in
    the rollback section.

## Findings

- NOTE: Andon term list quarterly review has no CI enforcement
  (architect-review item 7). Ceremony lives in ADR-004 §P5 as
  operator ownership. Not addressed here; deferred as a follow-on
  in the WU-4 PR body.
- NOTE: Substrate bundling + Vernon anticorruption pattern
  (reading lite-manifest.json) deferred to a later workunit per
  decomp L82-92. tier-filter.mjs is pre-shaped so the manifest
  read slots in at scripts/tier-filter.mjs L57 without a rewrite.
- NOTE: Single-reviewer Environment gate is an accepted risk with
  layered mitigations per ADR-004 §K3. Revisit trigger: a second
  maintainer joins the project. Repo-settings edit, no code.
- NOTE: Content-hash algorithm from ADR-003 is semver-locked at
  0.0.2 as noted in the WU-3 reviewer marker. WU-4 does not touch
  the hash; ADR-004 stacks on top of ADR-003.

acknowledged: true

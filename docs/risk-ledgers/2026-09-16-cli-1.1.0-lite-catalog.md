---
tier: project
title: Risk ledger — cli 1.1.0 bundle the lite catalog
goal: docs/iteration-bets/2026-09-16-cli-1.1.0-lite-catalog.md
date: 2026-09-16
mode: pre-mortem light (Klein) + adversarial /rfc (outside lenses)
authoring_luminaries:
  primary:
    - gary-klein  # pre-mortem originator
    - alistair-cockburn  # walking skeleton
    - linus-torvalds  # adopter contract
  supporting:
    - michael-feathers
    - jerome-saltzer-and-michael-schroeder
    - andy-hunt-and-dave-thomas
    - hyrum-wright
    - stephen-toulmin
    - rich-hickey
    - charles-sanders-peirce
---

# Risk ledger — cli 1.1.0 bundle the lite catalog

## Sources read

- Pre-mortem marker `state/markers/pre-mortem/feat-cli-1.1.0-lite-catalog.marker` — 3 lenses, top 3 folded
- `/rfc` adversarial run this session — 6 outside lenses
- `docs/iteration-bets/2026-09-16-cli-1.1.0-lite-catalog.md` — goal doc with 10-step sequencing
- Parent goal `docs/iteration-bets/2026-09-14b-lite-bundle-self-contained.md` at bassclef-upstream
- ADR-056 D1 self-containment invariant
- Cli 1.0.3 sister ledger — `docs/risk-ledgers/2026-09-16-cli-1.0.3-prepublish-recursive-hook-copy.md` — 11 primary + 11 RFC folds (reuse shape)

## Ledger

| ID | Description | Lens | P | S | Design mitigation (Step 1) | Impl mitigation (Step 6) | Verification |
|---|---|---|---|---|---|---|---|
| L1 | Adopter has files at target paths under `.claude/skills/` etc. | Linus | H | 🟡 | Walker refuses `AlreadyExists` per existing 1.0.4 behavior | Existing `writeSafely` refusal path; no change needed | Tier 0 test: pre-seed target file, run init, assert refused count > 0 |
| L2 | MINOR bump hides file-count shift from adopter reading changelog fast | Linus | M | 🟡 | Changelog opens with explicit "adds N files under `.claude/`" note | CHANGELOG.md 1.1.0 entry leads with count table | Step 9 PR body carries the note in Summary |
| L3 | Adopter runs own `bassclef-sync` pulling skills; 1.1.0 also lays them; conflict | Linus | M | 🟡 | Walker refuse-existing preserves adopter's vendored skills | Same refusal path as L1 covers | Step 10 smoke: cold adopter (no vendored files) refused count = 0 |
| F1 | Tier 0 tests hard-code 292 count; upstream refresh breaks cli | Feathers | H | 🟢 | Test contract: count via `manifest.entries.length` | RED tests import manifest, use `.entries.length` | Step 5 RED test grep: no numeric literal for entry count |
| F2 | Fixture manifest shape diverges from real v1.6.1 as upstream evolves | Feathers | M | 🟢 | Fixture is a real slice (5 entries per type) not hand-authored | Fixture generator reads real manifest, picks first N per type | Step 5 fixture test: fixture schema-major matches real manifest |
| SS1 | Path traversal via manifest `entry.path` (`../etc/passwd`) | Saltzer-Schroeder | L | 🔴 | Every entry runs through `classify()` mediation | Walker calls `classify({command: '$CLAUDE_PROJECT_DIR/' + entry.path}, opts)` | Step 5 RED test: entry.path with `..` throws `PathTraversalRefused` |
| SS2 | Symlink at target path for a rule/skill/luminary dir | Saltzer-Schroeder | L | 🟡 | Existing `writeSafely` S-fold covers all paths | No change; refusal message names readlink target | Step 5 RED test: pre-seed symlink at target, assert `SymlinkRefused` |
| SS3 | Adopter env HOME=/root without `--allow-root` | Saltzer-Schroeder | L | 🟡 | Existing `resolveHome` refusal covers all paths | No change; classify() calls resolveHome | Step 5 RED test: HOME=/root without --allow-root throws `SudoBypassRefused` |
| HT | Pragmatic coupling — literal counts in tests break on manifest refresh | Hunt-Thomas | H | 🟢 | Same as F1; combined mitigation | Same as F1 | Same as F1 |
| HW | Every path in bundle becomes adopter contract (Hyrum) | Hyrum-Wright | H | 🔴 | ADR-057 pins destination-path routing per type explicitly | ADR-057 authored in Step 6 same PR; walker code matches ADR verbatim | Step 8 architect-review #3 verifies walker routing matches ADR-057 table |
| Tm | "MINOR because restore" hides scope-shift signal to adopters | Toulmin | M | 🟡 | Changelog + PR body name file-count delta up front | CHANGELOG entry leads with count table (see L2) | Step 8 review verifies count table present |
| RH | Prepublish reads manifest at BUILD; walker acts at INIT — 2 moments, 1 contract | Rich Hickey | M | 🟡 | Manifest carries `schema_major`; cli fails loud at BOTH moments | Prepublish schema check + walker schema check share same lib | Step 5 RED tests for schema-major mismatch at both moments |
| Pc | "Regression" narrative vs "always hooks-only by design" — competing readings | Peirce | L | 🟢 | ADR-057 pins the intent so future readers cannot mistake | ADR-057 body cites both readings; explicitly names catalog-shipping as invariant | Step 8 review verifies ADR-057 addresses the competing reading |
| Ck | Walking skeleton — Steps 1-9 build layers; only Step 10 tests end-to-end | Cockburn | H | 🔴 | Step 10 dispatches real skill + lands real marker; non-negotiable ship gate | Cold-adopter script asserts skill dispatch, marker landing, luminary resolve, rule fire | Step 10 execution + green output pasted to session log |
| N1 | Manifest read fails silently at build time (Voyage / network / file-not-found) | Nygard | M | 🔴 | Fail-loud on missing manifest with structured error naming remediation | `CopyFailure` kind `ManifestMissing` fires with fix instructions | Step 5 RED test: missing manifest → exit code 4 + stderr names cure |

## Mitigation coverage

Every ledger row has a design entry, an implementation entry, and a verification entry. Rows without a Step 5 test are the exception cases the architect-review gates catch instead.

## Handoff to design (Step 1)

- Decomposition doc names the 4 module boundaries: catalog reader, walker classifier, init counter, preflight harness
- UC-init-lite-catalog carries the L1 refusal happy path AND the SS1-3 mediation extensions
- Interaction design carries the L2 banner-line + count-table shape
- ADR-057 pin (HW + Pc) captures both the destination-path table AND the competing-readings resolution

## Handoff to code (Step 6)

- Every ID above lands in a comment `// @risk: <ID>` at the code site that mitigates it
- `@risk` annotations grep-audit at Step 8 architect-review #3

## Follow-on tracked

- N1 severity 🔴 may warrant a dedicated Nygard row for network-fetch-time manifest reads (out of scope for 1.1.0 — manifest read is local sibling checkout only). File follow-on if a future release fetches manifest over HTTP.
